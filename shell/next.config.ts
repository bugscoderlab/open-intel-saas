import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Product shell (ticket 10). Upstream's frontend/ remains untouched
  // reference-to-port until Phase 2, so this app owns no remote aliases yet.
};

export default nextConfig;
